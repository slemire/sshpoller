language: python
python:
  - "2.7"

install:
  - pip install .
  - pip install -r requirements.txt
  - git clone https://github.com/google/textfsm
  - cd textfsm
  - python textfsm/setup.py install

script:
  - python test_sshpoller.py

notifications:
  email: false

sudo: false